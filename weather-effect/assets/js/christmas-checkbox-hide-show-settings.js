// All Checkbox
		// Ball Checkbox Show And Hide Settings Start
		var ball = jQuery( 'input[name="ball"]:checked' ).val();
if (jQuery( '#ball' ).is( ":checked" )) {
	jQuery( '.christmas_balls_sh' ).show();
} else {
	jQuery( '.christmas_balls_sh' ).hide();
}

		jQuery( document ).ready(
			function() {
				jQuery( 'input[name="ball"]' ).change(
					function(){
						var ball = jQuery( 'input[name="ball"]:checked' ).val();
						if (jQuery( this ).is( ":checked" )) {
							jQuery( '.christmas_balls_sh' ).show();
						} else {
							jQuery( '.christmas_balls_sh' ).hide();
						}
					}
				);
			}
		);
		// Ball Checkbox Show And Hide Settings	End

		// Bells Checkbox Show And Hide Settings Start
		var bell = jQuery( 'input[name="bell"]:checked' ).val();
		if (jQuery( '#bell' ).is( ":checked" )) {
			jQuery( '.christmas_bells_sh' ).show();
		} else {
			jQuery( '.christmas_bells_sh' ).hide();
		}

		jQuery( document ).ready(
			function() {
				jQuery( 'input[name="bell"]' ).change(
					function(){
						var bell = jQuery( 'input[name="bell"]:checked' ).val();
						if (jQuery( this ).is( ":checked" )) {
							jQuery( '.christmas_bells_sh' ).show();
						} else {
							jQuery( '.christmas_bells_sh' ).hide();
						}
					}
				);
			}
		);
		// Bells Checkbox Show And Hide Settings End

		// Candy Checkbox Show And Hide Settings Start
		var candy = jQuery( 'input[name="candy"]:checked' ).val();
		if (jQuery( '#candy' ).is( ":checked" )) {
			jQuery( '.christmas_candy_sh' ).show();
		} else {
			jQuery( '.christmas_candy_sh' ).hide();
		}

		jQuery( document ).ready(
			function() {
				jQuery( 'input[name="candy"]' ).change(
					function(){
						var candy = jQuery( 'input[name="candy"]:checked' ).val();
						if (jQuery( this ).is( ":checked" )) {
							jQuery( '.christmas_candy_sh' ).show();
						} else {
							jQuery( '.christmas_candy_sh' ).hide();
						}
					}
				);
			}
		);
		// Candy Checkbox Show And Hide Settings End

		// Gift Checkbox Show And Hide Settings Start
		var gift = jQuery( 'input[name="gift"]:checked' ).val();
		if (jQuery( '#gift' ).is( ":checked" )) {
			jQuery( '.christmas_gift_sh' ).show();
		} else {
			jQuery( '.christmas_gift_sh' ).hide();
		}

		jQuery( document ).ready(
			function() {
				jQuery( 'input[name="gift"]' ).change(
					function(){
						var gift = jQuery( 'input[name="gift"]:checked' ).val();
						if (jQuery( this ).is( ":checked" )) {
							jQuery( '.christmas_gift_sh' ).show();
						} else {
							jQuery( '.christmas_gift_sh' ).hide();
						}
					}
				);
			}
		);
		// Gift Checkbox Show And Hide Settings End

		// Hat Checkbox Show And Hide Settings Start
		var hat = jQuery( 'input[name="hat"]:checked' ).val();
		if (jQuery( '#hat' ).is( ":checked" )) {
			jQuery( '.christmas_hat_sh' ).show();
		} else {
			jQuery( '.christmas_hat_sh' ).hide();
		}

		jQuery( document ).ready(
			function() {
				jQuery( 'input[name="hat"]' ).change(
					function(){
						var hat = jQuery( 'input[name="hat"]:checked' ).val();
						if (jQuery( this ).is( ":checked" )) {
							jQuery( '.christmas_hat_sh' ).show();
						} else {
							jQuery( '.christmas_hat_sh' ).hide();
						}
					}
				);
			}
		);
		// Hat Checkbox Show And Hide Settings End

		// Ribbon Checkbox Show And Hide Settings Start
		var ribbon = jQuery( 'input[name="ribbon"]:checked' ).val();
		if (jQuery( '#ribbon' ).is( ":checked" )) {
			jQuery( '.christmas_ribbon_sh' ).show();
		} else {
			jQuery( '.christmas_ribbon_sh' ).hide();
		}

		jQuery( document ).ready(
			function() {
				jQuery( 'input[name="ribbon"]' ).change(
					function(){
						var ribbon = jQuery( 'input[name="ribbon"]:checked' ).val();
						if (jQuery( this ).is( ":checked" )) {
							jQuery( '.christmas_ribbon_sh' ).show();
						} else {
							jQuery( '.christmas_ribbon_sh' ).hide();
						}
					}
				);
			}
		);
		// Ribbon Checkbox Show And Hide Settings End

		// Santa Checkbox Show And Hide Settings Start
		var santa = jQuery( 'input[name="santa"]:checked' ).val();
		if (jQuery( '#santa' ).is( ":checked" )) {
			jQuery( '.christmas_santa_sh' ).show();
		} else {
			jQuery( '.christmas_santa_sh' ).hide();
		}

		jQuery( document ).ready(
			function() {
				jQuery( 'input[name="santa"]' ).change(
					function(){
						var santa = jQuery( 'input[name="santa"]:checked' ).val();
						if (jQuery( this ).is( ":checked" )) {
							jQuery( '.christmas_santa_sh' ).show();
						} else {
							jQuery( '.christmas_santa_sh' ).hide();
						}
					}
				);
			}
		);
		// Santa Checkbox Show And Hide Settings End

		// Santa Sleigh Checkbox Show And Hide Settings Start
		var santa_sleigh = jQuery( 'input[name="santa_sleigh"]:checked' ).val();
		if (jQuery( '#santa_sleigh' ).is( ":checked" )) {
			jQuery( '.christmas_santa_sleigh_sh' ).show();
		} else {
			jQuery( '.christmas_santa_sleigh_sh' ).hide();
		}

		jQuery( document ).ready(
			function() {
				jQuery( 'input[name="santa_sleigh"]' ).change(
					function(){
						var santa_sleigh = jQuery( 'input[name="santa_sleigh"]:checked' ).val();
						if (jQuery( this ).is( ":checked" )) {
							jQuery( '.christmas_santa_sleigh_sh' ).show();
						} else {
							jQuery( '.christmas_santa_sleigh_sh' ).hide();
						}
					}
				);
			}
		);
		// Santa Sleigh Checkbox Show And Hide Settings End

		// Snowman Checkbox Show And Hide Settings Start
		var snowman = jQuery( 'input[name="snowman"]:checked' ).val();
		if (jQuery( '#snowman' ).is( ":checked" )) {
			jQuery( '.christmas_snowman_sh' ).show();
		} else {
			jQuery( '.christmas_snowman_sh' ).hide();
		}

		jQuery( document ).ready(
			function() {
				jQuery( 'input[name="snowman"]' ).change(
					function(){
						var snowman = jQuery( 'input[name="snowman"]:checked' ).val();
						if (jQuery( this ).is( ":checked" )) {
							jQuery( '.christmas_snowman_sh' ).show();
						} else {
							jQuery( '.christmas_snowman_sh' ).hide();
						}
					}
				);
			}
		);
		// Snowman Checkbox Show And Hide Settings End

		// Sockes Checkbox Show And Hide Settings Start
		var socks = jQuery( 'input[name="socks"]:checked' ).val();
		if (jQuery( '#socks' ).is( ":checked" )) {
			jQuery( '.christmas_socks_sh' ).show();
		} else {
			jQuery( '.christmas_socks_sh' ).hide();
		}

		jQuery( document ).ready(
			function() {
				jQuery( 'input[name="socks"]' ).change(
					function(){
						var socks = jQuery( 'input[name="socks"]:checked' ).val();
						if (jQuery( this ).is( ":checked" )) {
							jQuery( '.christmas_socks_sh' ).show();
						} else {
							jQuery( '.christmas_socks_sh' ).hide();
						}
					}
				);
			}
		);
		// Socks Checkbox Show And Hide Settings End

		// Star Checkbox Show And Hide Settings Start
		var star = jQuery( 'input[name="star"]:checked' ).val();
		if (jQuery( '#star' ).is( ":checked" )) {
			jQuery( '.christmas_star_sh' ).show();
		} else {
			jQuery( '.christmas_star_sh' ).hide();
		}

		jQuery( document ).ready(
			function() {
				jQuery( 'input[name="star"]' ).change(
					function(){
						var star = jQuery( 'input[name="star"]:checked' ).val();
						if (jQuery( this ).is( ":checked" )) {
							jQuery( '.christmas_star_sh' ).show();
						} else {
							jQuery( '.christmas_star_sh' ).hide();
						}
					}
				);
			}
		);
		// Star Checkbox Show And Hide Settings End

		// snow_flake Checkbox Show And Hide Settings Start
		var snow_flake = jQuery( 'input[name="snow_flake"]:checked' ).val();
		if (jQuery( '#snow_flake' ).is( ":checked" )) {
			jQuery( '.christmas_snow_flake_sh' ).show();
		} else {
			jQuery( '.christmas_snow_flake_sh' ).hide();
		}

		jQuery( document ).ready(
			function() {
				jQuery( 'input[name="snow_flake"]' ).change(
					function(){
						var snow_flake = jQuery( 'input[name="snow_flake"]:checked' ).val();
						if (jQuery( this ).is( ":checked" )) {
							jQuery( '.christmas_snow_flake_sh' ).show();
						} else {
							jQuery( '.christmas_snow_flake_sh' ).hide();
						}
					}
				);
			}
		);
		// snow_flake Checkbox Show And Hide Settings End

